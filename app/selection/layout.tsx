export default function SelectionLayout({ children, }: { children: React.ReactNode }) {
    return (
        <main>{children}</main>
    )
}

export const metadata = {
    title: "reavol.com/selection",
    description: "reavol - trang web đọc sách"
}