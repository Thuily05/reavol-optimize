export const metadata = {
    title: "reavol.com/booknew",
    description: "reavol - trang web đọc sách"
}
export default function BookNewLayout({ children, }: { children: React.ReactNode }) {
    return (
        <main>{children}</main>
    )
}